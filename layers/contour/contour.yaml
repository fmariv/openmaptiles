layer:
  id: "contour"
  description: |
      The contour layer contains the contour lines of Catalonia.
  buffer_size: 4
  fields:
    icgc_id:
      description:
          Identificador únic de l'ICGC.
    class:
      description: |
          Use the **class** to differentiate between different contour lines.
    height:
      description: Pendent d'informar.
    name:
      description: Pendent.
  datasource:
    geometry_field: geometry
    query: (SELECT icgc_id, geometry, class, height, name FROM layer_contour(!bbox!, z(!scale_denominator!), !pixel_width!)) AS t
schema:
  - ./contour.sql
